# Core LLM & Graph orchestration
langgraph>=0.2.30
langchain>=0.2.15
langchain-openai>=0.1.22

# Data validation / schemas
pydantic>=2.7.0

# Environment variable management
python-dotenv>=1.0.1

# JSON robustness (optional but recommended for production)
orjson>=3.10.7

# LLM Provider Dependencies (install based on your chosen provider)
# For OpenAI and Azure OpenAI (already included via langchain-openai)
openai>=1.0.0

# For Anthropic Claude (install if LLM_PROVIDER=anthropic)
# anthropic>=0.18.0

# For Google Gemini (install if LLM_PROVIDER=gemini)
# google-generativeai>=0.3.0

# For Hugging Face (install if LLM_PROVIDER=huggingface)
# HuggingFace has multiple modes - install based on your configuration:
#
# 1. API mode only (HF_USE_LOCAL=false, HF_USE_LANGCHAIN=false):
#    huggingface-hub>=0.20.0
#
# 2. API mode with LangChain (HF_USE_LOCAL=false, HF_USE_LANGCHAIN=true):
#    huggingface-hub>=0.20.0
#    langchain-huggingface>=0.0.1
#
# 3. Local mode only (HF_USE_LOCAL=true, HF_USE_LANGCHAIN=false):
#    transformers>=4.30.0
#    torch>=2.0.0
#    accelerate>=0.20.0  # Optional: for faster loading
#
# 4. Local mode with LangChain (HF_USE_LOCAL=true, HF_USE_LANGCHAIN=true):
#    transformers>=4.30.0
#    torch>=2.0.0
#    langchain-huggingface>=0.0.1
#    accelerate>=0.20.0  # Optional: for faster loading

# For Ollama (uses OpenAI-compatible API, no extra dependencies needed)
